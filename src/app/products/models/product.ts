export interface Product {
    id: number;
    title: string;
    price: number; // Change the type to number for the price property
    category: string;
    quantite:number;
    description: string;
    image: string;
}